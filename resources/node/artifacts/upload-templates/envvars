KUBE_VERSION=${KUBE_VERSION}
VAULT_IMAGE=vault:${VAULT_RELEASE}
CNI_PLUGIN_URL=${CNI_PLUGIN_URL}
CLUSTER_NAME=${CLUSTER_NAME}
CONFIG_BUCKET=${AWS_ACCOUNT}-${CLUSTER_NAME}-config

# for pki call and cert alt_names
ROUTE53_ZONE_NAME=${ROUTE53_ZONE_NAME}
CLUSTER_INTERNAL_ZONE=${CLUSTER_INTERNAL_ZONE}
KUBE_API_SERVICE=${KUBE_API_SERVICE}
KUBE_API_DNSNAME=${KUBE_API_DNSNAME}
VAULT_CACERT=/opt/etc/vault/ca/ca.pem
VAULT_ADDR=https://vault.${CLUSTER_INTERNAL_ZONE}
